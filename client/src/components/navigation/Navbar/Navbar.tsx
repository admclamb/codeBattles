import React from 'react';
import { Link } from 'react-router-dom';
type Props = {};

const Navbar = (props: Props) => {
  return <nav className="bg-darker text-light"></nav>;
};
export default Navbar;
